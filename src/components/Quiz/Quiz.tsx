import './Quiz.module.css';

export type QuizProps = {
  props: string;
};

export const Quiz = ({ props }: QuizProps): JSX.Element => {
  return (
    <>
      <h1>FrontEnd Quiz</h1>
    </>
  );
};
